# Keep this file safe to commit. Never place real keys in it.
PORT=3000

# Optional: enables Gemini semantic matching after deterministic local matching fails.
# Leave blank to run getRyt entirely from the local verified evidence pack.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite
GEMINI_SEMANTIC_MATCHING_ENABLED=false
